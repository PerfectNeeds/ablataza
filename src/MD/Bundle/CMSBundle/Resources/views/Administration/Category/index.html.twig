{% extends 'AdminTemplateBundle::base.html.twig' %}
{% set page_title %}Categories Level 2{% endset %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('bundles/admintemplate/assets/plugins/datatable/css/bootstrap.datatable.min.css') }}" rel="stylesheet">
{% endblock %}
{% block body -%}
    {% if errors is defined %}
        <div class="alert alert-danger alert-block fade in alert-dismissable">
            <button aria-hidden="true" data-dismiss="alert" class="close" type="button">×</button>
            <strong>Oh snap!</strong> {{ errors }}
        </div>

    {% endif %}
    <div class="the-box text-right rounded">
        <a class="btn btn-warning" href="{{ path('supercategory') }}"><i class="fa fa-backward"></i>Back</a>
    </div>
    <div class="the-box">
        <div class="table-responsive">
            <table class="table table-striped table-hover" id="datatable-example">
                <thead class="the-box dark full">
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for entity in entities %}
                        <tr>
                            <td>{{ entity.id }}</td>
                            <td>{{ entity.name }}</td>
                            <td>
                                <a href="{{ path('subcategory', { 'pId': entity.id }) }}" class="btn btn-warning" title="List"><i class="fa fa-list"></i></a>
                                <a href="{{ path('category_edit', { 'id': entity.id }) }}" class="btn btn-info" data-toggle="tooltip" title="Edit"><i class="fa fa-pencil"></i></a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset('bundles/admintemplate/assets/plugins/datatable/js/jquery.dataTables.min.js') }}"></script>
    <script type="text/javascript" src="{{ asset('bundles/admintemplate/assets/plugins/datatable/js/bootstrap.datatable.js') }}"></script>
{% endblock %}